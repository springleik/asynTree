# hostTarget

Simulated target device exposes a TCP socket interface with a simple command interpreter. Separate host program acts as a client, interacting with the target and running an AST interpreter.
