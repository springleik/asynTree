# asynTree
Abstract Syntax Tree (AST) interpreters in C language.

Initial implementation is a skeleton interpreter framework, which could be developed into a small domain-specific language. Assuming we use TCP sockets for inter-process communication, this will be done for Linux/Unix targets first.
