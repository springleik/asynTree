# asynTree
Abstract Syntax Tree (AST) interpreters in Java.

Java implementation has not yet begun. Eventual goal is to support both host and target functionality in Java command line executables using TCP sockets for inter-process communication.
