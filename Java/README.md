# Abstract Syntax Tree (AST) interpreter in Java

Java implementation has not yet begun. Eventual goal is to support both host and target functionality in Java command line programs, using TCP sockets for inter-process communication.
